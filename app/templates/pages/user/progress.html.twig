{% extends '_base.html.twig' %}

{% block content %}
    {{ include('pages/user/_nav.html.twig') }}

    <div class="tab-pane" role="tabpanel">
        {{ form_errors(form) }}
        {{ form_start(form) }}
        {{ form_row(form.nations) }}
        {{ form_row(form.drivers) }}
        {{ form_row(form.save) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
